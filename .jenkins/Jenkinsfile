@Library('c3i')
import jfrog.c3i.C3iEngine

def artifactoryURL = "https://conan.jfrog.io/conan"
def credential = "c3i_on_artifactory"


def engine = new C3iEngine(this, artifactoryURL, credential, String winTmpPath)
engine.runBuild()
